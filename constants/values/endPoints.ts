const host = "https://api.staging.voxikids.com"
const login = host + "/security/login"
const passReset = host + "/account/reset_password"
const appointments = host + "/account/business/patients"

export default {
  login,
  passReset,
  appointments
}
